# CTD Load Step

Use this example when a downward counter should count production steps and support a preset load command.

- current signal path -> `CTD.CD`
- load branch -> `CTD.LD`
- preset value -> `PV`

Compile on a clone before reusing it in a live project.
