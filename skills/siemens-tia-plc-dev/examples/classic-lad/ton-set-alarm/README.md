# TON Set Alarm

Source case:

- `D:\path\to\project\PLC_Code\changes\fc1-conveyor-jam-ton-reuse-instance\network15.json`

What it shows:

- one series condition chain
- one `TON` action feeding the next signal stage
- one `SET` output action
- a real imported and compiled alarm network on `FC1_报警程序块`

Use it after the single-coil drill when you want to practice timer-backed alarm networks.
