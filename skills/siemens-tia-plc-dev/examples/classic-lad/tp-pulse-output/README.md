# TP Pulse Output

Use this example when one command should generate a fixed-width pulse.

- current signal path -> `TP.IN`
- pulse width -> `PT`
- `TP.Q` -> downstream `COIL`

Compile on a clone before reusing it in a live project.
