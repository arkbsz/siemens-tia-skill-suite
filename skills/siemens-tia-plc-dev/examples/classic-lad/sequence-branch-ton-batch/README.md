# Sequence Branch TON Batch

Use this example when one automatic station block needs a small verified batch that mixes:

- branch-driven motion step logic
- direct outputs on a shared prefix
- timer-driven dwell steps with `TON + MOVE`

This example mirrors a validated three-network slice of the FC5 automatic block.
