# TOF Delayed Dropout

Use this example when an output should stay true for a short time after the source signal drops.

- current signal path -> `TOF.IN`
- off-delay time -> `PT`
- `TOF.Q` -> downstream `COIL`

Compile on a clone before reusing it in a live project.
